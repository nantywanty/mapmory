scalar GraphQLDate

type Location {
    lng: Float!
    lat: Float!
}

type Photo {
    filename: String!
    id: String!
    location: Location!
    date: String!
}

type User {
    _id: ID!
    userId: String!
    photos: [Photo!]
}

##### Top level declarations
type Query {
    getUser(id: String!): User
    getAll: [User]
}

type Mutation {
    addUser: User!
    setUser: User!
}